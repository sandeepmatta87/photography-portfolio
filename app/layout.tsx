import type { Metadata } from "next";
import "./globals.css";
import Navigation from "@/components/Navigation";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "SM Photography | Event Photography",
  description:
    "Premium event photography capturing your most precious moments. Weddings, corporate events, celebrations — crafted with an editorial eye.",
  keywords: "event photography, wedding photographer, corporate photographer, portrait, editorial",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Navigation />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
